[phases.setup]
nixPkgs = ["php84", "php84Packages.composer"]

[phases.build]
cmds = [
  "php -m",
  "composer install --optimize-autoloader --no-interaction"
]

[start]
cmd = "php -d extension=intl -d extension=zip artisan serve --host=0.0.0.0 --port=$PORT"
``
